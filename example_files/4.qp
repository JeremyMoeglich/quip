fn test() { 
    let add = (a, b) -> a + b;
    assert_eq!(add(2, 3), 5);
}

fn test2 {
    // named arguments
    let add = (a, b) -> a + b;
    assert_eq!(add {
        a: 3,
        b: 2,
    }, 5);
}