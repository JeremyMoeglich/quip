fn test() { 
    // example of currying
    let add = |a, b| a + b;
    let add_one = add>3+2<;
    assert_eq!(add_one(2), 3);
}

fn test2 {
    // named arguments
    let add = |a, b| a + b;
    assert_eq!(add{
        a: 3,
        b: 2,
    }, 5);
}