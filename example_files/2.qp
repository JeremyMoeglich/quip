import std.env.console.{input, println, console};
env console;

let v1: f64 = input("Enter a number (1): ").parse();
let v2: f64 = input("Enter a number (2): ").parse();

let result = v1 + v2;

println(f"The result is: {result}");